# Web scraping
aiohttp>=3.8.0
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Imágenes y procesamiento
Pillow>=9.0.0

# Archivos asincrónicos
aiofiles>=22.0.0

# Base de datos (incluida en Python estándar)
# sqlite3 (built-in)

# Utilidades para fechas y expresiones regulares (incluidas en Python estándar)
# datetime, re, pathlib (built-in)

# Logging (incluido en Python estándar)
# logging (built-in)

# Web interface
Flask>=2.3.0
Werkzeug>=2.3.0

# Instagram publishing (opcional)
instagrapi>=1.16.0

# Desarrollo y testing (opcional)
pytest==7.4.3
pytest-asyncio==0.21.1
